server:
  port: 8000
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/RLucasLeo/Microservicios
          search-paths: config-data
  application:
    name: config-service